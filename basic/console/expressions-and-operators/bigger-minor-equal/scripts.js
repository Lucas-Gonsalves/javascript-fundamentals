let balance = 500;
let payment = 120;

// > bigger to
console.log(balance > payment);

// < minor to
console.log(balance < payment);

// >=  bigger or equal to
balance = 120;
console.log(balance >= payment);

// >=  minor or equal to
balance = 500
console.log(balance <= payment);

// A constant creates a variable whose value is fixed (cannot be changed).
const number = 42;
console.log(number);

// This will generate an error because the value cannot be changed.
number = 55;
console.log(number);

/**
 * When a programming language is case-sensitive,
 * it means that it distinguishes between uppercase and lowercase letters.
 *
 * For example: "Lucas" is considered different of "lucas"
 */

var name = "lucas";
var Name = "Test";

console.log(name);
console.log(Name);

// conditional ternary operator

let age = 16;
console.log(
  age > 18 // condition 
  ? "You can drive." // if true
  : "You can't drive." // if false
);

console.log(
  age < 18 // condition 
  ? "You can't drive." // if true
  : "You can drive." // if false
);
